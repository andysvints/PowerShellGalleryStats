{
  "bindings": [
    {
      "authLevel": "function",
      "type": "httpTrigger",
      "direction": "in",
      "name": "Request",
      "methods": [
        "get"
      ]
    },
    {
      "type": "http",
      "direction": "out",
      "name": "Response"
    },
    { 
    "name": "PSDocuments", 
    "type": "cosmosDB", 
    "direction": "in", 
    "databaseName": "psgallerystats", 
    "containerName": "psgallery", 
    "sqlQuery": "SELECT c.id, c.IconUrl, c.Owners, c.Description, c.LicenseUrl, c.ProjectUrl, c.DownloadCount,c.Scoring, c.cp_TotalScore, c.cp_SourceCodeScore, c.cp_MetadataScore, c.Tags, c.Published, c.CompanyName, c.Version FROM c WHERE c.id = {module}", 
    "connection": "CosmosDB",
    "identity": "systemAssigned"
   }
  ]
}