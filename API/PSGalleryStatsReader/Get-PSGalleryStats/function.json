{
  "bindings": [
    {
      "authLevel": "function",
      "type": "httpTrigger",
      "direction": "in",
      "name": "Request",
      "methods": [
        "get"
      ]
    },
    {
      "type": "http",
      "direction": "out",
      "name": "Response"
    },
    { 
    "name": "PSDocuments", 
    "type": "cosmosDB", 
    "direction": "in", 
    "databaseName": "psgallerystats", 
    "containerName": "psgallery", 
    "sqlQuery": "SELECT c.id, c.IconUrl, c.Owners, c.Description, c.LicenseUrl, c.ProjectUrl, c.DownloadCount,c.Scoring, c.cp_TotalScore, c.cp_SourceCodeScore, c.cp_MetadataScore, c.Tags FROM c WHERE REGEXMATCH(LOWER(c.id), LOWER({module}))", 
    "connection": "CosmosDB",
    "identity": "systemAssigned"
  }
  ]
}
