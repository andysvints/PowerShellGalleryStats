{
  "bindings": [
    {
      "authLevel": "function",
      "type": "httpTrigger",
      "direction": "in",
      "name": "Request",
      "methods": [
        "get"
      ]
    },
    {
      "type": "http",
      "direction": "out",
      "name": "Response"
    },
    { 
    "name": "PSDocuments", 
    "type": "cosmosDB", 
    "direction": "in", 
    "databaseName": "psgallerystats", 
    "containerName": "psgallery", 
    "sqlQuery": "SELECT c.id, c.IconURL, c.Owners, c.Description, c.LicenseURL, c.ProjectURL, c.DownloadCount, c.Scoring, c.cp_TotalScore, c.cp_SourceCodeScore, c.cp_MetadataScore FROM c WHERE REGEXMATCH(LOWER(c.id), LOWER({module}))", 
    "connection": "CosmosDB",
    "identity": "systemAssigned"
  }
  ]
}
